# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (KEEP SECRET - Server-side only)
# WARNING: This key bypasses Row Level Security. Never expose in client-side code.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Session Secret (KEEP SECRET)
# Generate a cryptographically secure random secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your-cryptographically-secure-random-secret-here

# Admin PINs (KEEP SECRET)
# Change these from the defaults before deploying to production
ADMIN_PIN_CHASE=your-chase-admin-pin
ADMIN_PIN_SHELDON=your-sheldon-admin-pin

# Optional: Node Environment
NODE_ENV=development
